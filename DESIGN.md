# menma - CLIベースのファイル管理アプリ 設計書

## 1. 概要
React Ink を使用して開発される CLI ベースのファイル管理ツールです。
`menma add {タイトル} {プロジェクト名}` コマンドを通じて、指定されたフォーマットでファイルを生成し、タグ付けを行い、設定されたエディタで開く機能を提供します。

## 2. 機能要件

### 2.1. `menma add {タイトル} {プロジェクト名}` コマンド
- **タイトル**: 生成されるファイルのタイトル。ファイル名の一部やファイル内容のヘッダーに使用されます。
- **プロジェクト名**: ファイルが属するプロジェクト。ファイルの保存パスの一部に使用されます。

#### 2.1.1. フォーマット選択
- コマンド実行後、利用可能なファイルフォーマット（例: 議事録、メモなど）をリスト表示し、ユーザーに選択させます。
- フォーマットは設定ファイルで定義されます。

#### 2.1.2. タグ入力
- フォーマット選択後、任意でタグを入力できるプロンプトを表示します。
- 複数のタグをカンマ区切りなどで入力可能にします。

#### 2.1.3. ファイル生成
- 選択されたフォーマットと入力されたタイトル、プロジェクト名、タグに基づいてファイルを生成します。
- ファイルの保存場所は設定ファイルで定義されるベースディレクトリ配下です。
- ファイル名にはタイトルとタイムスタンプなどを含めます。
- ファイル内容には、タイトル、プロジェクト名、タグ、作成日時などのメタデータをヘッダーとして含めます（フォーマットによる）。

#### 2.1.4. ファイルオープン
- ファイル生成後、設定ファイルで指定されたコマンド（例: `code`, `vim`, `open` など）で生成されたファイルを開きます。

## 3. 設定ファイル (`menma.config.json` または `menma.config.js`)

- アプリケーションの動作をカスタマイズするための設定ファイルです。
- ユーザーの~/.config配下に設置されることを想定しています。

### 3.1. 設定項目

- `baseDir`: 生成されるファイルを保存するベースディレクトリのパス。
- `formats`: 利用可能なファイルフォーマットの定義。
    - `name`: フォーマット名（例: "議事録", "メモ"）
    - `extension`: ファイル拡張子（例: "md", "js"）
    - `template`: ファイルの初期内容のテンプレート。プレースホルダー（例: `{{title}}`, `{{project}}`, `{{tags}}`, `{{date}}`）を使用可能です。
- `editorCommand`: 生成されたファイルを開くためのコマンド。`{{filePath}}` プレースホルダーを使用可能です。

### 3.2. 設定ファイルの例

```json
{
  "baseDir": "./menma-files",
  "formats": [
    {
      "name": "議事録",
      "extension": "md",
      "template": "# {{title}}\n\nProject: {{project}}\nTags: {{tags}}\nDate: {{date}}\n\n---\n\n"
    },
    {
      "name": "メモ",
      "extension": "js",
      "template": "// Title: {{title}}\n// Project: {{project}}\n// Tags: {{tags}}\n// Date: {{date}}\n\n"
    }
  ],
  "editorCommand": "code {{filePath}}"
}
```

## 4. ディレクトリ構成案

```
menma-cli/
├── src/
│   ├── index.tsx           # メインエントリポイント (Inkアプリケーション)
│   ├── cli.ts              # コマンドライン引数解析とメインロジック
│   ├── components/         # Inkコンポーネント
│   │   ├── FormatSelector.tsx
│   │   └── TagInput.tsx
│   ├── utils/              # ユーティリティ関数
│   │   ├── fileGenerator.ts
│   │   └── configLoader.ts
│   └── types.ts            # 型定義
├── menma.config.json       # 設定ファイルの例
├── package.json
├── tsconfig.json
└── README.md
```

## 5. 主要コンポーネント/モジュールの設計

### 5.1. `cli.ts`
- `commander` や `yargs` のようなライブラリを使用してコマンドライン引数を解析します。
- `menma add` コマンドのハンドリングを行います。
- Inkアプリケーションの起動と終了を行います。

### 5.2. `index.tsx`
- Inkアプリケーションのルートコンポーネントです。
- `cli.ts` から渡された引数に基づいて、`FormatSelector` や `TagInput` などのコンポーネントをレンダリングします。
- ユーザーの入力（フォーマット、タグ）を受け取り、ファイル生成ロジックに渡します。

### 5.3. `components/FormatSelector.tsx`
- `ink-select-input` などのライブラリを使用して、フォーマット選択のUIを提供します。
- 設定ファイルから読み込んだフォーマットリストを表示します。
- 選択されたフォーマットを親コンポーネントに返します。

### 5.4. `components/TagInput.tsx`
- `ink-text-input` などのライブラリを使用して、タグ入力のUIを提供します。
- ユーザーがタグを入力し、Enterで確定します。
- 入力されたタグを親コンポーネントに返します。

### 5.5. `utils/configLoader.ts`
- `menma.config.json` を読み込み、パースします。
- 設定ファイルのバリデーションを行います。
- 設定オブジェクトを返します。

### 5.6. `utils/fileGenerator.ts`
- 選択されたフォーマット、タイトル、プロジェクト名、タグ、設定ファイルの情報を受け取ります。
- ファイルパスを生成します（例: `baseDir/project-name/YYYYMMDD_HHmmss_title.extension`）。
- テンプレートをレンダリングし、プレースホルダーを実際の値に置換します。
- ファイルを書き込みます。

## 6. 開発環境とビルド

- **言語**: TypeScript
- **ランタイム**: Node.js
- **UIフレームワーク**: React Ink
- **パッケージマネージャー**: npm または yarn
- **ビルドツール**: Webpack または Rollup (Inkアプリケーションのバンドル用)

## 7. 今後の拡張性（考慮事項）

- `menma list`: 生成されたファイルを一覧表示する機能。
- `menma search`: タグやタイトルでファイルを検索する機能。
- `menma open`: 既存のファイルを指定して開く機能。
- テンプレートの外部ファイル化。
- Git連携（ファイルの追加を自動でコミットなど）。
